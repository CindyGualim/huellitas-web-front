
  # Set Branding with Logo

  This is a code bundle for Set Branding with Logo. The original project is available at https://www.figma.com/design/Lf0FnkRkDP3H46gXnYjUMX/Set-Branding-with-Logo.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  